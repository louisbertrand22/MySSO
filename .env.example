# Server Configuration
PORT=3000
NODE_ENV=development

# Base URL - The public URL where the MySSO backend is accessible
# This is used for JWT issuer and server console logs
# For local development: http://localhost:3000
# For production (e.g., Render): https://mysso-wq3e.onrender.com
BASE_URL=http://localhost:3000

# Database Configuration
# For local PostgreSQL: postgresql://postgres:postgres@localhost:5432/mysso?schema=public
# For Supabase: postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mysso?schema=public

# JWT Configuration
# JWT_ISSUER: If not set, will default to BASE_URL
# JWT_ISSUER=http://localhost:3000
JWT_EXPIRATION=3600
JWT_SECRET=changeme

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Example: https://mysso-wq3e.onrender.com,https://app.example.com,http://localhost:5173
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:3002,http://localhost:3001

# Keys Directory (optional)
# Directory where RSA keys are stored
# KEYS_DIR=./keys
