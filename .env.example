# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration (comma-separated list of allowed origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Database
# For local development with Docker:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mysso?schema=public"

# For Supabase (recommended for production):
# Get your connection string from Supabase Dashboard > Project Settings > Database > Connection String
# Use the "Connection pooling" URI for better performance and connection management
# Example format:
# DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres"
# Or with connection pooling (recommended):
# DATABASE_URL="postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mysso?schema=public"

# JWT Configuration
JWT_ISSUER=http://localhost:3000
JWT_EXPIRATION=3600

# Keys Directory
KEYS_DIR=./keys

# JWT Secret for authentication (bcrypt-based)
JWT_SECRET="qsdfQSDF65465qQ54FFF"

# OAuth2 Configuration
# Note: In production, set NODE_ENV=production to enforce HTTPS for cookies
# Add allowed redirect URIs in src/services/authCodeService.ts
