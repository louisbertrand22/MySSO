# Server Configuration
PORT=3000
NODE_ENV=development

# Base URL - The public URL where the MySSO backend is accessible
# This is used for JWT issuer and OpenID Connect discovery endpoint
# IMPORTANT: Must be set correctly for production!
# For local development: http://localhost:3000
# For production (e.g., Render): https://your-app-name.onrender.com
# Note: Use HTTPS in production for security
BASE_URL=http://localhost:3000

# Database Configuration
# For local PostgreSQL: postgresql://postgres:postgres@localhost:5432/mysso?schema=public
# For Supabase: postgresql://postgres.[YOUR-PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mysso?schema=public

# JWT Configuration
# JWT_ISSUER: If not set, will default to BASE_URL
# JWT_ISSUER=http://localhost:3000
JWT_EXPIRATION=3600
# JWT_SECRET: IMPORTANT! Change this to a strong random secret in production!
# Generate with: openssl rand -base64 32
JWT_SECRET=changeme

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# IMPORTANT: Must be set in production to restrict which domains can access your API
# Include your frontend URL(s) and any client applications
# Example: https://your-frontend.onrender.com,https://app.example.com
# For development: http://localhost:3000,http://localhost:5173,http://localhost:3002,http://localhost:3001
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:3002,http://localhost:3001

# Keys Directory (optional)
# Directory where RSA keys are stored
# KEYS_DIR=./keys
